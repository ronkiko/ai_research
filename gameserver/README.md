# GameServer

`gameserver/` is the standalone multiplayer-server laboratory. The active
architecture is [`v1/`](v1/README.md).

It is intentionally separate from the future sibling `gameclient/` project.
