# Transport

Internal Console channel implementations for CONTROL ingress and STATE,
TELEMETRY, and EVENTS publishing. Transport is not a Player API; Player uses
only `contracts/` framing and contracts.

There is no standalone process entrypoint. Local documentation: `doc/`.
