# Scripted Player

Deterministic external Player used by the integration smoke. It sends public
Joystick decisions and waits for public ACKs.

It has no Console implementation dependency. Entrypoint: `main.py`. Local
documentation: `doc/`.
