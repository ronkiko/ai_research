# Player And Model

Human, scripted, MLP, PPO, and LLM runtimes are replaceable external Players.
A model is a kind of Player, not a Console subsystem. Model output adapters map
their decisions to the public two-button Joystick contract.
