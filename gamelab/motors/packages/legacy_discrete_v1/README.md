# legacy_discrete_v1

Archived pre-continuous three-action Motor. It is stored as a package so a future
bot configurator can inspect/migrate it, but it is deliberately incompatible
with the current GameServer continuous physical-effort body and cannot be mounted
by the active Motor registry.
