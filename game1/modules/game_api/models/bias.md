## Одно число-привычка

**1 параметр (b), без входа** — для игр без состояния.

### Схема

    b ── σ ──→ p

### Формула

    logit = b

Модель ничего не видит — просто ставит вслепую. Единственное число b ползёт к
логиту частоты, с которой мир выпадает в единицу. Подходит для Ball и Drift.
