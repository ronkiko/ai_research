"""Режимы обучения адаптера ИИ и справка по ним.

Режим = как train() превращает исход хода в изменение весов. Справка (help) —
бытовое объяснение для оператора, без математики; движок отдаёт её в UI для
окна выбора/листания режимов.
"""
from __future__ import annotations

from modules.base import TrainModeInfo

# Ключи режимов.
SUPERVISED = "supervised"
RL = "rl"
PLAY = "play"  # без обучения: модель играет выученными весами, не учится
ADAPTIVE = "rl+adaptive"  # RL + детектор смены мира

# Справка по каждому режиму: бытовым языком, как объяснение настольной игры.
SUPERVISED_HELP = (
    "Модель учится «с ответом» (кросс-энтропия). Каждый ход мир показывает "
    "правильный ответ — что реально выпало. Модель подстраивает свои числа так, "
    "чтобы её предсказание совпадало с этим ответом, а действие — просто "
    "«выбираю то, что предсказал» (argmax, без случайности).\n\n"
    "Учится быстро и устойчиво. На мяче и кормушке «угадать мир» = «выиграть», "
    "поэтому модель показывает максимум игры.\n\n"
    "НО: если мир врёт, чтобы обмануть (дилер), supervised упрямо учится "
    "угадывать карту — и проигрывает (точность 0%, фишки −1 за ход). Здесь "
    "«предсказать мир» ≠ «выиграть», и supervised тупит."
)

RL_HELP = (
    "Модель учится «по подкреплению» (REINFORCE policy gradient), без ответа. "
    "Мир не показывает правильный ответ — только платит фишку за выигрыш или "
    "забирает за проигрыш. Модель действует случайно (с вероятностью, которую "
    "сама подбирает), и со временем повышает шанс действий, что приносили "
    "фишки, и снижает шанс невыгодных.\n\n"
    "Если какое-то действие явно выгоднее, модель становится всё увереннее — "
    "вероятность растёт к 100%. Без страховки это приводит к зацикливанию "
    "(policy saturation: модель перестаёт пробовать другие ходы, даже если "
    "правила поменялись). Чтобы этого избежать, встроен entropy bonus — "
    "небольшой «страх уверенности», который не даёт вероятности упереться "
    "в 0% или 100% и оставляет щель для разведки.\n\n"
    "Учится шумнее и медленнее supervised, зато не требует правильного ответа. "
    "На дилере RL находит контрстратегию: ставить против карты → 96% побед "
    "(supervised там сливает 0%)."
)

PLAY_HELP = (
    "Модель просто играет тем, что уже выучила, и ничего не меняет в своих "
    "числах. Действие выбирается argmax: модель всегда делает то, что считает "
    "лучшим, без случайных проб (нет ε-разведки, нет entropy).\n\n"
    "Режим для оценки: «как поведёт себя уже обученная модель без дообучения». "
    "Если модель не училась — argmax при нулевых весах даёт p=0.5, то есть "
    "всегда право (70% на мяче, 34% на дилере). Если предварительно обучить "
    "в другом режиме, а потом переключить на Play — видно, чему модель "
    "реально научилась, без шума обучения."
)

ADAPTIVE_HELP = (
    "RL + детектор смены мира. В основе — тот же REINFORCE с entropy bonus, "
    "что и в обычном RL. Но модель дополнительно следит за своей точностью "
    "на скользящем окне (100 последних ходов). Если точность падает ниже 52% — "
    "модель считает, что мир изменил правила, и временно (на 100 ходов) "
    "включает форсаж: втрое быстрее учится и вшестеро активнее пробует "
    "непривычные ходы (разведка 30% вместо 5%).\n\n"
    "На стационарных играх (мяч, кормушка, дилер) форсаж не включается — "
    "точность стабильно выше порога. На «Плывущем мяче» (ветер меняется "
    "каждые 300 ходов) детектор срабатывает и помогает быстрее "
    "переучиться. Эффект скромный: Bias +3% точности, Logistic помогает "
    "слабо — он и так сам подстраивается через контекст."
)

TRAIN_MODES: list[TrainModeInfo] = [
    TrainModeInfo(
        key=SUPERVISED,
        title="Supervised — с ответом",
        summary="учится по правильному ответу, который раскрывает мир",
        help=SUPERVISED_HELP,
    ),
    TrainModeInfo(
        key=RL,
        title="RL — по подкреплению",
        summary="учится по фишкам, пробуя ходы вслепую, без ответа",
        help=RL_HELP,
    ),
    TrainModeInfo(
        key=ADAPTIVE,
        title="RL+Adaptive — RL + детектор смены мира",
        summary="RL, но ускоряется, когда мир меняет правила",
        help=ADAPTIVE_HELP,
    ),
    TrainModeInfo(
        key=PLAY,
        title="Play — без обучения",
        summary="играет выученными весами, не учится во время прогона",
        help=PLAY_HELP,
    ),
]


def mode_info(key: str) -> TrainModeInfo | None:
    for m in TRAIN_MODES:
        if m.key == key:
            return m
    return None


def is_valid(key: str) -> bool:
    return any(m.key == key for m in TRAIN_MODES)